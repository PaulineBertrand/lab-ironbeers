<div class="one-beer">
    <img src={{image_url}} alt="beer picture">
    <div class="beer-text">
        <h2>{{name}}</h3>
        <h4>{{tagline}}</h4>
        <p>{{description}}</p>
    </div>
</div>